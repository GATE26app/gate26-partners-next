import { Grid, GridItem } from '@chakra-ui/react';

import styled from '@emotion/styled';

export const PickerGrid = styled(Grid)``;

export const PickerGridItem = styled(GridItem)`
  display: flex;
  align-items: center;
  justify-content: center;
  div {
    height: 44px;
  }
`;
