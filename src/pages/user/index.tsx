import withAdminLayout from '@components/common/@Layout/AdminLayout';

function UserManagement({ Component, pageProps }: any) {
  return <div>어드민 : 이용자</div>;
}

export default withAdminLayout(UserManagement);
